pub class Counter {
    pub var n: int;
    pub Counter(start: int) {
        self.n = start;
    }
    pub fn bump() { self.n = self.n + 1; }
    pub fn get() { ret self.n; }
}

var c = Counter(0);
for i in 0..100000 {
    c.bump();
    c.bump();
    c.bump();
}
print(c.get());
