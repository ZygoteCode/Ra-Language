// std.io — minimal I/O helpers built on top of the `print` built-in.

pub fn println(msg) {
    print(msg);
}

pub fn printall(items: list) {
    for item in items {
        print(item);
    }
}

pub const NEWLINE = "\n";
