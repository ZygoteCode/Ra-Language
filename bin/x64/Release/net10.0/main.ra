pub interface Named {
    fn get_name();
}

pub class Person impl Named {
    pub var name: string;

    pub fn get_name() {
        ret self.name;
    }
}

fn greet(x: Named) {
    print(x.get_name());
}

var p = Person();
p.name = "Alessandro";
greet(p);