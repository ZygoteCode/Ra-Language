pub class Counter {
    pub static var count: int = 0;

    pub static fn inc() {
        Counter.count = Counter.count + 1;
    }
}

pub class Math {
    pub static fn sum(a: int, b: int) {
        ret a + b;
    }
}

Counter.inc();
print(Counter.count);
print(Math.sum(2, 3));