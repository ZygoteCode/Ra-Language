// imports_helpers/strings_lib.ra — string helpers for import tests.

pub fn repeat(s, n) {
    var out = "";
    var i = 0;
    while i < n {
        out = out + s;
        i = i + 1;
    }
    ret out;
}

pub const GREETING = "hello";
